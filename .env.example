# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True
FLASK_PORT=5000

# Model Configuration  
MODEL_PATH_BUG=classifiers/bug/model/
MODEL_PATH_FEATURE=classifiers/feature/model/
SIMILARITY_MODEL_PATH=similarity/model/sbert.pt

# Application Settings
MAX_REQUESTS_PER_PAGE=10
DEFAULT_CONFIDENCE_THRESHOLD=0.5

# Optional: Database Configuration (for future use)
# DATABASE_URL=sqlite:///requests.db

# Optional: API Keys (for future integrations)
# GITHUB_TOKEN=your_github_token_here
